# Q-UNITY V10 依赖清单
# 安装: pip install -r requirements.txt

# 核心数值计算
numpy>=1.24
pandas>=2.0

# 数据源（主力+备用）
akshare>=1.12.0
adata>=2.9.5          # [FIX-E-04] 新增：巨潮节点，主力数据源，防封IP

# 参数优化
optuna>=3.0

# 加速（可选，强烈推荐）
numba>=0.57

# 终端颜色（可选）
colorama>=0.4
